function buildCoachPlayerProfile(player){const data=player||{},title=String(data.name||'').trim()||'Spelare',position=String(data.position||'').trim(),normalizedPosition=position.toLocaleLowerCase('sv-SE'),badges=[];if(normalizedPosition.includes('målvakt'))badges.push('MÅLVAKT');if(data.isCaptain)badges.push('KAPTEN');return{title,shirtNumber:String(data.shirtNumber||'').trim(),position,badges,avatarUrl:String(data.avatarUrl||'').trim()};}
function buildCoachPlayerPageViewModel(name){const profile=buildCoachPlayerProfile({name});return{title:profile.title,backLabel:'← Tillbaka till spelaröversikten',subtitle:'Utveckling just nu · Återkoppling · Bedömning · Historik'};}
function buildCoachPlayerNavigation(){return[{label:'Utveckling just nu',target:'coachPlayerContext'},{label:'Återkoppling',target:'coachFocusFeedbackControls'},{label:'Bedömning',target:'coachPlayerDevelopment'},{label:'Historik',target:'coachHistorySection'}];}
function buildCoachPlayerLeaderTools(){return[{label:'NYTT FOKUS',target:'coachPlayerContext',mode:'focus'},{label:'GE FEEDBACK',target:'coachFocusFeedbackControls',mode:'feedback'},{label:'HANTERA MÅL',target:'coachPlayerContext',mode:'goal'}];}
function playerProfileFromButton(button){if(!button)return buildCoachPlayerProfile({});const card=button.closest&&button.closest('[data-player-id]'),data=button.dataset||{},cardData=card&&card.dataset?card.dataset:{};return buildCoachPlayerProfile({name:button.textContent,shirtNumber:data.shirtNumber||cardData.shirtNumber||'',position:data.position||cardData.position||'',isCaptain:data.isCaptain==='true'||cardData.isCaptain==='true',avatarUrl:data.avatarUrl||cardData.avatarUrl||''});}
function ensureCoachPlayerPageHeader(){const coachView=document.getElementById('coachDevelopmentView'),development=document.getElementById('coachPlayerDevelopment');if(!coachView||!development)return null;let header=document.getElementById('coachPlayerPageHeader');if(header)return header;header=document.createElement('section');header.id='coachPlayerPageHeader';header.className='coach-player-page-header';header.hidden=true;development.parentNode.insertBefore(header,development);return header;}
function scrollToCoachPlayerSection(targetId){let target=document.getElementById(targetId);if(!target&&targetId==='coachHistorySection')target=document.querySelector('.coach-history-section');if(!target)return false;target.scrollIntoView({behavior:'smooth',block:'start'});return true;}
function closeCoachPlayerQuickTool(){if(typeof document==='undefined')return false;const coachView=document.getElementById('coachDevelopmentView'),shell=document.getElementById('coachPlayerQuickTool');if(coachView)coachView.classList.remove('coach-player-quick-tool-open');if(shell)shell.remove();return Boolean(coachView||shell);}
function openCoachPlayerQuickTool(tool,profile){if(typeof document==='undefined'||!tool)return false;closeCoachPlayerQuickTool();let target=document.getElementById(tool.target);if(!target&&tool.target==='coachHistorySection')target=document.querySelector('.coach-history-section');if(!target)return false;const coachView=document.getElementById('coachDevelopmentView');if(!coachView)return false;const shell=document.createElement('section');shell.id='coachPlayerQuickTool';shell.className='coach-player-quick-tool';shell.dataset.quickTool=tool.mode||'';const bar=document.createElement('header');bar.className='coach-player-quick-tool-bar';const copy=document.createElement('div');copy.innerHTML='<small>LEDARVERKTYG</small><strong>'+String(profile&&profile.title||'Spelare')+'</strong><span>'+tool.label+'</span>';const close=document.createElement('button');close.type='button';close.textContent='STÄNG';close.addEventListener('click',closeCoachPlayerQuickTool);bar.appendChild(copy);bar.appendChild(close);shell.appendChild(bar);const stage=document.createElement('div');stage.className='coach-player-quick-tool-stage';const marker=document.createComment('coach-player-quick-tool-origin');target.parentNode.insertBefore(marker,target);stage.appendChild(target);shell.appendChild(stage);coachView.appendChild(shell);coachView.classList.add('coach-player-quick-tool-open');shell._restore=function(){if(marker.parentNode)marker.parentNode.insertBefore(target,marker);marker.remove();};const originalRemove=shell.remove.bind(shell);shell.remove=function(){if(shell._restore)shell._restore();originalRemove();};shell.scrollIntoView({behavior:'smooth',block:'start'});const focusable=target.querySelector&&target.querySelector('textarea,input,button,summary');if(focusable)setTimeout(function(){focusable.focus();},0);return true;}
function openCoachPlayerPage(button){const coachView=document.getElementById('coachDevelopmentView'),header=ensureCoachPlayerPageHeader();if(!coachView||!header||!button)return;const profile=playerProfileFromButton(button),navigation=buildCoachPlayerNavigation(),leaderTools=buildCoachPlayerLeaderTools(),meta=[profile.shirtNumber?'#'+profile.shirtNumber:'',profile.position].filter(Boolean).join(' · '),avatar=profile.avatarUrl?`<img class="coach-player-profile-avatar" src="${profile.avatarUrl}" alt="">`:'<div class="coach-player-profile-avatar coach-player-profile-avatar-empty" aria-hidden="true">BILD<br>KOMMER</div>';header.innerHTML=`<button type="button" class="coach-player-page-back">← Tillbaka</button><div class="coach-player-profile-hero">${avatar}<div class="coach-player-profile-copy"><span class="coach-player-profile-kicker">SPELARPROFIL</span><h2>${profile.title}</h2>${meta?`<p class="coach-player-profile-meta">${meta}</p>`:''}${profile.badges.length?`<div class="coach-player-profile-badges">${profile.badges.map(function(badge){return `<span>${badge}</span>`;}).join('')}</div>`:''}</div></div><section class="coach-player-leader-tools" aria-label="Ledarverktyg"><span>LEDARVERKTYG</span><div>${leaderTools.map(function(item){return `<button type="button" data-target="${item.target}" data-quick-tool="${item.mode}">${item.label}</button>`;}).join('')}</div></section><nav class="coach-player-section-nav" aria-label="Spelarens utvecklingsdelar">${navigation.map(function(item){return `<button type="button" data-target="${item.target}">${item.label}</button>`;}).join('')}</nav>`;header.hidden=false;coachView.classList.add('coach-player-detail-open');const back=header.querySelector('.coach-player-page-back');if(back)back.addEventListener('click',closeCoachPlayerPage,{once:true});header.querySelectorAll('[data-target]').forEach(function(navButton){navButton.addEventListener('click',function(){if(navButton.dataset.quickTool){const tool=leaderTools.find(function(item){return item.mode===navButton.dataset.quickTool;});openCoachPlayerQuickTool(tool,profile);return;}scrollToCoachPlayerSection(navButton.dataset.target);});});header.scrollIntoView({behavior:'smooth',block:'start'});}
function closeCoachPlayerPage(){const coachView=document.getElementById('coachDevelopmentView'),header=document.getElementById('coachPlayerPageHeader'),development=document.getElementById('coachPlayerDevelopment');if(!coachView)return;closeCoachPlayerQuickTool();coachView.classList.remove('coach-player-detail-open');if(header){header.hidden=true;header.replaceChildren();}if(development)development.replaceChildren();const search=document.getElementById('coachRosterSearch');if(search)search.focus();coachView.scrollIntoView({behavior:'smooth',block:'start'});}
function setupCoachPlayerPage(){const list=document.getElementById('coachPlayerList');if(!list||list.dataset.playerPageReady==='true')return false;list.dataset.playerPageReady='true';list.addEventListener('click',function(event){const button=event.target.closest('.coach-player-button');if(!button)return;openCoachPlayerPage(button);});ensureCoachPlayerPageHeader();return true;}
function waitForCoachPlayerPage(){if(setupCoachPlayerPage())return;setTimeout(waitForCoachPlayerPage,100);}
if(typeof module!=='undefined'&&module.exports)module.exports={buildCoachPlayerProfile,buildCoachPlayerPageViewModel,buildCoachPlayerNavigation,buildCoachPlayerLeaderTools,openCoachPlayerQuickTool,closeCoachPlayerQuickTool};
if(typeof window!=='undefined'&&typeof document!=='undefined')waitForCoachPlayerPage();