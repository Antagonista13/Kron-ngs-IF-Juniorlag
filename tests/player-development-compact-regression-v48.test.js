const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('phone history buttons use compact padding',()=>{assert.match(fs.readFileSync('player-development-compact.css','utf8'),/padding:8px 4px/);});