const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history renderer updates controls after panels',()=>{const js=fs.readFileSync('player-development-layout.js','utf8');const fn=js.slice(js.indexOf('function showHistory'),js.indexOf('function ensureHistory'));assert.ok(fn.indexOf('querySelectorAll')>fn.indexOf("['goal','focus','development']"));});