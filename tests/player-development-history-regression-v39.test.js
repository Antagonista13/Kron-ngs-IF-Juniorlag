const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history visibility is set before panel append',()=>{const js=fs.readFileSync('player-development-layout.js','utf8');const fn=js.slice(js.indexOf("['goal','focus','development']"),js.indexOf("document.querySelectorAll('#playerUnifiedHistory"));assert.ok(fn.indexOf('el.hidden=')<fn.indexOf('content.appendChild(el)'));});