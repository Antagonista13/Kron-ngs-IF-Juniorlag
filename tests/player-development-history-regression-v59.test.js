const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history state is mutable for tab switching',()=>{assert.match(fs.readFileSync('player-development-layout.js','utf8'),/let activeHistory/);});