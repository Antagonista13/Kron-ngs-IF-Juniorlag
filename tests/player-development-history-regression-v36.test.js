const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history render considers all known panels',()=>{assert.match(fs.readFileSync('player-development-layout.js','utf8'),/\['goal','focus','development'\]/);});