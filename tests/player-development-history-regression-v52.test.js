const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('moveExisting does not reset active history to array index zero',()=>{const js=fs.readFileSync('player-development-layout.js','utf8');const move=js.slice(js.indexOf('function moveExisting'));assert.doesNotMatch(move,/index!==0/);assert.match(move,/showHistory\(activeHistory\)/);});