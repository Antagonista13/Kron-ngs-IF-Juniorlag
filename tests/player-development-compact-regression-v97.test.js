const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('inactive history control uses light gray background',()=>{assert.match(fs.readFileSync('player-development-compact.css','utf8'),/background:#ececec;color:#111/);});