const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history click delegates to showHistory',()=>{assert.match(fs.readFileSync('player-development-layout.js','utf8'),/addEventListener\('click',\(\)=>showHistory/);});