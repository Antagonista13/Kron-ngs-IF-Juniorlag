const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('mobile player development uses tighter spacing',()=>{const css=fs.readFileSync('player-development-compact.css','utf8');assert.match(css,/@media\(max-width:560px\)/);assert.match(css,/#playerDevelopmentJourney\{gap:14px\}/);assert.match(css,/development-card\{padding:14px 16px/);});