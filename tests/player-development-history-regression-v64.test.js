const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history click listener passes semantic panel key',()=>{assert.match(fs.readFileSync('player-development-layout.js','utf8'),/showHistory\(btn\.dataset\.historyPanel\)/);});