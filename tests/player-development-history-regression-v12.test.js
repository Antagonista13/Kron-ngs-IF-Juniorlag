const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('unified history card is appended to history slot',()=>{assert.match(fs.readFileSync('player-development-layout.js','utf8'),/historySlot\.appendChild\(card\)/);});