const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history content starts close to controls',()=>{assert.match(fs.readFileSync('player-development-compact.css','utf8'),/#playerHistoryContent\{margin-top:12px\}/);});