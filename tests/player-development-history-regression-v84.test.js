const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history only moves node when it is outside content container',()=>{assert.match(fs.readFileSync('player-development-layout.js','utf8'),/if\(el\.parentElement!==content\)content\.appendChild\(el\)/);});