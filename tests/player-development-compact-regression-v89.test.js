const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('assessment card radius is eighteen pixels',()=>{assert.match(fs.readFileSync('player-development-compact.css','utf8'),/border-radius:18px/);});