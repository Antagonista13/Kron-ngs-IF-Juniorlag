const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history tabs expose selected state',()=>{const js=fs.readFileSync('player-development-layout.js','utf8');assert.match(js,/role="tablist"/);assert.match(js,/aria-selected/);});