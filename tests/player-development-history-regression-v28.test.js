const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history render iterates semantic panel keys',()=>{assert.match(fs.readFileSync('player-development-layout.js','utf8'),/\['goal','focus','development'\]\.forEach/);});