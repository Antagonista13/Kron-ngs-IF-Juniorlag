const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('showHistory records selection before rendering targets',()=>{const js=fs.readFileSync('player-development-layout.js','utf8');const fn=js.slice(js.indexOf('function showHistory'),js.indexOf('function ensureHistory'));assert.ok(fn.indexOf('activeHistory=kind')<fn.indexOf('historyTarget(name)'));});