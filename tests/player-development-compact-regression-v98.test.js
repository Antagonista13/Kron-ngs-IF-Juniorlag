const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('nested history content radius is smaller than outer card',()=>{assert.match(fs.readFileSync('player-development-compact.css','utf8'),/border-radius:16px/);});