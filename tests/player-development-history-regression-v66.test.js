const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history source nodes are moved not copied',()=>{assert.match(fs.readFileSync('player-development-layout.js','utf8'),/appendChild\(el\)/);});