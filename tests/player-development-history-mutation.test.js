const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('mutation observer reapplies selected history rather than first history',()=>{const js=fs.readFileSync('player-development-layout.js','utf8');assert.match(js,/MutationObserver\(moveExisting\)/);assert.match(js,/showHistory\(activeHistory\)/);});