const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history targets existing goal focus and development sources',()=>{const js=fs.readFileSync('player-development-layout.js','utf8');for(const id of ['developmentGoalHistory','developmentFocusHistory','developmentHistory','profileDevelopmentHistory'])assert.match(js,new RegExp(id));});