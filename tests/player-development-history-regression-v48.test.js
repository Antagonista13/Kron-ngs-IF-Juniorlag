const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history target lookup supports profile fallback',()=>{assert.match(fs.readFileSync('player-development-layout.js','utf8'),/document\.getElementById\(alt\[kind\]\|\|''\)/);});