const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history controls install click handlers',()=>{assert.match(fs.readFileSync('player-development-layout.js','utf8'),/querySelectorAll\('\[data-history-panel\]'\)\.forEach/);});