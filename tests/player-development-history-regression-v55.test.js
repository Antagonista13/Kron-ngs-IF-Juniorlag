const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('development update event still invokes layout setup',()=>{assert.match(fs.readFileSync('player-development-layout.js','utf8'),/kronang:development-updated/);});