const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('fs');
const { canManageTeamPosts, canManageSpecificTeamPost, canViewTeamPosts, validateTeamPost, formatTeamPostDate, sortTeamPosts, buildTeamPostEditState, normalizeTeamPostImageUrl, selectHomeNewsPosts, teamPostAuthorLabel } = require('../team-posts.js');
const { handleTeamPostNavigation } = require('../team-post-navigation-reset.js');
test('only coach and admin roles can create team posts',()=>{assert.equal(canManageTeamPosts('coach'),true);assert.equal(canManageTeamPosts('admin'),true);assert.equal(canManageTeamPosts('player'),false);assert.equal(canManageTeamPosts('parent'),false);assert.equal(canManageTeamPosts('pending'),false);});
test('post ownership follows the locked admin coach hierarchy',()=>{assert.equal(canManageSpecificTeamPost('admin','admin'),true);assert.equal(canManageSpecificTeamPost('admin','coach'),true);assert.equal(canManageSpecificTeamPost('coach','coach'),true);assert.equal(canManageSpecificTeamPost('coach','admin'),false);assert.equal(canManageSpecificTeamPost('player','coach'),false);assert.equal(canManageSpecificTeamPost('parent','coach'),false);});
test('parent can read team posts while pending cannot',()=>{assert.equal(canViewTeamPosts('parent'),true);assert.equal(canViewTeamPosts('player'),true);assert.equal(canViewTeamPosts('pending'),false);});
test('requires both title and body for a team post',()=>{assert.deepEqual(validateTeamPost('','Text'),{valid:false,message:'Skriv en rubrik.'});assert.deepEqual(validateTeamPost('Rubrik',''),{valid:false,message:'Skriv ett meddelande.'});assert.deepEqual(validateTeamPost(' Rubrik ',' Text '),{valid:true,title:'Rubrik',body:'Text'});});
test('formats team post dates in Swedish',()=>assert.equal(formatTeamPostDate('2026-09-03T08:00:00Z'),'3 september 2026'));
test('pinned posts sort before newer normal posts',()=>{const posts=[{id:'new',is_pinned:false,created_at:'2026-09-03T10:00:00Z'},{id:'pin',is_pinned:true,created_at:'2026-09-01T10:00:00Z'},{id:'old',is_pinned:false,created_at:'2026-09-01T09:00:00Z'}];assert.deepEqual(sortTeamPosts(posts).map(p=>p.id),['pin','new','old']);});
test('home keeps important separate and avoids duplicating it as latest',()=>{const posts=[{id:'important',is_pinned:true,created_at:'2026-09-05T15:00:00Z'},{id:'normal',is_pinned:false,created_at:'2026-09-05T14:00:00Z'},{id:'older-pin',is_pinned:true,created_at:'2026-09-04T10:00:00Z'}];assert.deepEqual(selectHomeNewsPosts(posts),{important:posts[0],latest:posts[1]});assert.deepEqual(selectHomeNewsPosts([posts[0],posts[2]]),{important:posts[0],latest:posts[2]});});
test('important posts use Team Manager while normal posts omit author',()=>{assert.equal(teamPostAuthorLabel({is_pinned:true,author_role:'admin',author_name:'Henric Bergqvist'}),'TEAM MANAGER');assert.equal(teamPostAuthorLabel({is_pinned:false,author_role:'admin',author_name:'Henric Bergqvist'}),'');assert.equal(teamPostAuthorLabel({is_pinned:true,author_role:'coach',author_name:'Coach'}),'TEAM MANAGER');});
test('normalizes optional image urls',()=>{assert.equal(normalizeTeamPostImageUrl('  https://example.com/photo.jpg  '),'https://example.com/photo.jpg');assert.equal(normalizeTeamPostImageUrl(null),'');});
test('edit state preserves author role and optional image url',()=>assert.deepEqual(buildTeamPostEditState({id:'abc',title:' Träning ',body:' Kom i tid ',is_pinned:true,image_url:' https://example.com/a.jpg ',author_role:'coach'}),{id:'abc',title:'Träning',body:'Kom i tid',isPinned:true,imageUrl:'https://example.com/a.jpg',authorRole:'coach'}));
test('home contains separate important and latest news cards',()=>{const html=fs.readFileSync('index.html','utf8');assert.match(html,/id="homeImportantNewsCard"/);assert.match(html,/id="importantNews"/);assert.match(html,/id="homeNewsCard"/);assert.match(html,/id="latestNews"/);assert.match(html,/team-posts\.js\?v=8/);});
test('important home card has prominent gold alert treatment',()=>{const html=fs.readFileSync('index.html','utf8'),css=fs.readFileSync('home-important-news.css','utf8');assert.match(html,/class="home-important-alert-icon"[^>]*>!<\/span>/);assert.match(html,/class="home-important-label">VIKTIGT<\/span>/);assert.match(css,/\.home-important-news-card\s*\{[^}]*border:\s*2px solid #d7a72f/i);assert.match(css,/\.home-important-alert-icon\s*\{[^}]*border-radius:\s*50%[^}]*background:/i);assert.match(css,/\.home-important-label\s*\{[^}]*background:/i);assert.match(css,/\.home-important-news-card \.home-card-linkhint\s*\{[^}]*color:\s*#7b5b12/i);assert.match(html,/home-important-news\.css\?v=1/);});
test('team modules refresh together when Laget is opened',()=>{const focus=fs.readFileSync('team-focus.js','utf8');assert.match(focus,/kronang:team-page-opened/);const posts=fs.readFileSync('team-posts.js','utf8');assert.match(posts,/kronang:team-page-opened/);});
test('publish button matches the gold framed leader tool buttons',()=>{const css=fs.readFileSync('team-posts.css','utf8'),index=fs.readFileSync('index.html','utf8');assert.match(css,/\.team-post-submit\{[^}]*border:2px solid #7b632f[^}]*border-radius:12px[^}]*background:#111/i);assert.ok(index.includes('team-posts.css?v=11'));});
test('team post composer closes when a new app session starts',()=>{const source=fs.readFileSync('team-posts.js','utf8');assert.match(source,/kronang:app-start-reset/);assert.match(source,/teamPostForm[^\n]*hidden\s*=\s*true/);});
test('leaving Laget closes and resets an open team post composer',()=>{const fields={'#teamPostTitle':{value:'Test'},'#teamPostBody':{value:'Text'},'#teamPostPinned':{checked:true},'#teamPostImage':{value:'fake.jpg'},'#teamPostImagePreview':{innerHTML:'<img>'},'#saveTeamPost':{textContent:'PUBLICERAR…'},'#teamPostMessage':{textContent:'Test'},'#teamPostForm':{hidden:false}};const composer={dataset:{currentImageUrl:'x'},querySelector:s=>fields[s]||null};const doc={getElementById:id=>id==='teamPostComposer'?composer:null};assert.equal(handleTeamPostNavigation('homePage',doc),true);assert.equal(fields['#teamPostForm'].hidden,true);assert.equal(fields['#teamPostTitle'].value,'');assert.equal(fields['#teamPostBody'].value,'');assert.equal(fields['#teamPostPinned'].checked,false);assert.equal(composer.dataset.currentImageUrl,'');assert.equal(handleTeamPostNavigation('teamPage',doc),false);assert.ok(fs.readFileSync('index.html','utf8').includes('team-post-navigation-reset.js?v=1'));});
