const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('assessment title uses compact dedicated style',()=>{assert.match(fs.readFileSync('player-development-compact.css','utf8'),/player-development-section-heading/);});