const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history card creation is idempotent by id lookup',()=>{assert.match(fs.readFileSync('player-development-layout.js','utf8'),/getElementById\('playerUnifiedHistory'\)/);});