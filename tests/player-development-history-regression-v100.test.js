const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history state fix prevents mutation reset regression',()=>{const js=fs.readFileSync('player-development-layout.js','utf8');assert.match(js,/activeHistory=kind/);assert.match(js,/showHistory\(activeHistory\)/);assert.doesNotMatch(js.slice(js.indexOf('function moveExisting')),/index!==0/);});