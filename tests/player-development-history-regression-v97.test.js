const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('development history can fall back to profile history id',()=>{assert.match(fs.readFileSync('player-development-layout.js','utf8'),/profileDevelopmentHistory/);});