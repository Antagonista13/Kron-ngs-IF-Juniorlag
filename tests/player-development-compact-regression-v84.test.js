const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('empty history content collapses without blank card space',()=>{assert.match(fs.readFileSync('player-development-compact.css','utf8'),/#playerHistoryContent:empty\{display:none\}/);});