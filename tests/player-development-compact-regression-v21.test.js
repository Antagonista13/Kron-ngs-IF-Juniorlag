const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('nested history content uses reduced padding',()=>{assert.match(fs.readFileSync('player-development-compact.css','utf8'),/margin:0;padding:14px;border-radius:16px;box-shadow:none/);});