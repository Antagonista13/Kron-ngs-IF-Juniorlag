const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('mutation updates reuse current history selection',()=>{const js=fs.readFileSync('player-development-layout.js','utf8');assert.match(js,/function moveExisting\(\)[\s\S]*showHistory\(activeHistory\)/);});