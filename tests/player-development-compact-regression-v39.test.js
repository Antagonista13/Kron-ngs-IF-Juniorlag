const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('nested history content uses smaller radius',()=>{assert.match(fs.readFileSync('player-development-compact.css','utf8'),/padding:14px;border-radius:16px/);});