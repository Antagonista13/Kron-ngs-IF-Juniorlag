const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history title uses tight bottom margin',()=>{assert.match(fs.readFileSync('player-development-compact.css','utf8'),/margin:0 0 14px/);});