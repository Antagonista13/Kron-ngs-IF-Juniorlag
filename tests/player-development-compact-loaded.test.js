const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('compact stylesheet is linked in head',()=>{const html=fs.readFileSync('index.html','utf8');assert.ok(html.indexOf('player-development-compact.css?v=1')<html.indexOf('</head>'));});