const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('mobile journey gap is tighter than desktop',()=>{const css=fs.readFileSync('player-development-compact.css','utf8');assert.ok(css.includes('#playerDevelopmentJourney{gap:14px}'));});