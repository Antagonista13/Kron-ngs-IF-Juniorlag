const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history buttons expose goal focus development keys',()=>{const js=fs.readFileSync('player-development-layout.js','utf8');['goal','focus','development'].forEach(k=>assert.ok(js.includes('data-history-panel="'+k+'"')));});