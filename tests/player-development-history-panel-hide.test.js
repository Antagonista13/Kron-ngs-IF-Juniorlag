const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('inactive history panels are hidden',()=>{assert.match(fs.readFileSync('player-development-layout.js','utf8'),/el\.hidden=name!==activeHistory/);});