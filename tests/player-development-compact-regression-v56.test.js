const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history remains a standard card with compact override',()=>{assert.match(fs.readFileSync('player-development-layout.js','utf8'),/card player-unified-history/);});