const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history renderer compares every panel to one active key',()=>{assert.match(fs.readFileSync('player-development-layout.js','utf8'),/name!==activeHistory/);});