const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('assessment heading uses compact type',()=>{assert.match(fs.readFileSync('player-development-compact.css','utf8'),/font-size:14px;font-weight:900/);});