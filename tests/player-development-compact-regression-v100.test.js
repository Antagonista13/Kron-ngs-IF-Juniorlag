const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('compact density regression contract covers cards journey and history',()=>{const css=fs.readFileSync('player-development-compact.css','utf8');assert.match(css,/development-card\{padding:14px 16px/);assert.match(css,/#playerDevelopmentJourney\{gap:14px\}/);assert.match(css,/player-unified-history\{padding:16px\}/);});