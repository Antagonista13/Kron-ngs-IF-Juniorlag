const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('selected history renders after setup and updates',()=>{const js=fs.readFileSync('player-development-layout.js','utf8');assert.match(js,/moveExisting\(\);/);assert.match(js,/showHistory\(activeHistory\)/);});