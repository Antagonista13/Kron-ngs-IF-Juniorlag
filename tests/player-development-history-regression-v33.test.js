const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('profile history fallback is scoped to development history',()=>{assert.match(fs.readFileSync('player-development-layout.js','utf8'),/const alt=\{development:'profileDevelopmentHistory'\}/);});