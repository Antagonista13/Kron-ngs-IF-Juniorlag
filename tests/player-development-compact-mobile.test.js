const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('mobile compact contract reduces journey gap card padding and textarea height',()=>{const css=fs.readFileSync('player-development-compact.css','utf8');assert.match(css,/#playerDevelopmentJourney\{gap:14px\}/);assert.match(css,/development-card\{padding:14px 16px\}/);assert.match(css,/textarea\{min-height:64px\}/);});