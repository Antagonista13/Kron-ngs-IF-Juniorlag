const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history source fallback safely uses empty id when unavailable',()=>{assert.match(fs.readFileSync('player-development-layout.js','utf8'),/alt\[kind\]\|\|''/);});