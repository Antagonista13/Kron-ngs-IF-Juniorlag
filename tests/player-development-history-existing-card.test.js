const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history card is reused instead of duplicated',()=>{assert.match(fs.readFileSync('player-development-layout.js','utf8'),/let card=document\.getElementById\('playerUnifiedHistory'\)/);});