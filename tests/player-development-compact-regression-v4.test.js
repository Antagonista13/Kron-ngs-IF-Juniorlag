const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('compact changes preserve journey order',()=>{assert.match(fs.readFileSync('player-development-layout.js','utf8'),/\[goalSlot,focusSlot,assessmentSlot,trendSlot,historySlot\]/);});