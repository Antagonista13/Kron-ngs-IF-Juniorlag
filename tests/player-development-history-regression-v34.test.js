const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('history selection state lives in layout setup scope',()=>{const js=fs.readFileSync('player-development-layout.js','utf8');assert.ok(js.indexOf("let activeHistory='goal'")>js.indexOf('function setupPlayerDevelopmentLayout'));});