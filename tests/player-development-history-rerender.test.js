const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('moveExisting renders current history state',()=>{const js=fs.readFileSync('player-development-layout.js','utf8');assert.match(js,/ensureHistory\(\);showHistory\(activeHistory\);/);});