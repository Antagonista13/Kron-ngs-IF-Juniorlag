const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('compact development contract includes density and working history state',()=>{const css=fs.readFileSync('player-development-compact.css','utf8');const js=fs.readFileSync('player-development-layout.js','utf8');assert.match(css,/player-development-compact/);assert.match(js,/activeHistory/);assert.match(js,/showHistory\(activeHistory\)/);});