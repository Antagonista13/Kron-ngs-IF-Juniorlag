const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');test('goal is a valid history panel key',()=>{assert.match(fs.readFileSync('player-development-layout.js','utf8'),/data-history-panel="goal"/);});