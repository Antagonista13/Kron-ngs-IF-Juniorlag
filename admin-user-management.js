(function(root){
  function styles(){if(document.getElementById('adminUserManagementStyles'))return;const s=document.createElement('style');s.id='adminUserManagementStyles';s.textContent='.admin-user-groups{display:grid;gap:18px}.admin-user-group>h4{margin:0 0 8px;font-size:12px;letter-spacing:.12em;color:#777}.admin-user-group-list{display:grid;gap:10px}.admin-user-delete{margin-top:8px;background:#fff!important;color:#9d1c1c!important;border:1px solid #d9b0b0!important}.admin-user-card.locked .admin-user-delete{display:none}';document.head.appendChild(s);}
  function classify(card){const role=card.querySelector('[data-field="role"]');return role&&role.value==='player'?'player':'staff';}
  function addDelete(card){if(card.classList.contains('locked')||card.querySelector('[data-action="delete"]'))return;const b=document.createElement('button');b.type='button';b.className='admin-user-delete';b.dataset.action='delete';b.textContent='RADERA';b.addEventListener('click',async()=>{const id=card.dataset.userId;if(!id||!root.kronangSupabase)return;if(!confirm('Radera användarkontot? Spelarens profil och utvecklingshistorik sparas.'))return;b.disabled=true;b.textContent='RADERAR…';const {error}=await root.kronangSupabase.rpc('admin_delete_user',{p_profile_id:id});if(error){b.disabled=false;b.textContent='RADERA';const m=card.querySelector('.admin-card-message');if(m)m.textContent='Kunde inte radera kontot.';return;}card.remove();organize();});card.appendChild(b);}
  function organize(){const host=document.getElementById('adminUsers');if(!host||host.dataset.organizing==='1')return;host.dataset.organizing='1';styles();const cards=[...host.querySelectorAll(':scope > .admin-user-card'),...host.querySelectorAll('.admin-user-group-list > .admin-user-card')];if(!cards.length){host.dataset.organizing='0';return;}let wrap=host.querySelector('.admin-user-groups');if(!wrap){host.innerHTML='';wrap=document.createElement('div');wrap.className='admin-user-groups';wrap.innerHTML='<section class="admin-user-group"><h4>SPELARE</h4><div class="admin-user-group-list" data-admin-users="player"></div></section><section class="admin-user-group"><h4>STAB</h4><div class="admin-user-group-list" data-admin-users="staff"></div></section>';host.appendChild(wrap);}const player=wrap.querySelector('[data-admin-users="player"]'),staff=wrap.querySelector('[data-admin-users="staff"]');cards.forEach(card=>{addDelete(card);(classify(card)==='player'?player:staff).appendChild(card);});host.dataset.organizing='0';}
  function start(){const host=document.getElementById('adminUsers');if(!host)return;new MutationObserver(()=>setTimeout(organize,0)).observe(host,{childList:true,subtree:true});organize();}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();
  if(typeof module!=='undefined'&&module.exports)module.exports={classify};
})(typeof window!=='undefined'?window:globalThis);
